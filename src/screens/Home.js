import { StyleSheet, View } from "react-native";

import Category from "./Category";

const Home = () => {
  return (
    <View>
      <Category />
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
